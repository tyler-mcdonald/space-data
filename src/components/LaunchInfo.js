export const LaunchInfo = ({ data }) => {
  const launch = data[0];
  // image
  // craft
  // mission description
  // mission date
  // mission location
  // mission status (success/fail)
  // status message
  //

  return <img src={launch.image} alt="_________" />;
};
